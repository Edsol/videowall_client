generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:../database.db"
}

model Display {
  id      Int     @id @default(autoincrement())
  port    String
  primary Boolean
  width   Int
  height  Int
}
